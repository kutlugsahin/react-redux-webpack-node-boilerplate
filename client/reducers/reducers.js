var appReducer = (state = [], action) => {
    switch (action.type) {
        case 'APP_RDC':
            return state;
            break;
        default:
            return state;
    }
}

export {appReducer};
